// store.js
import { reactive, ref } from 'vue';

export const allFiles = reactive({});
export const trashFiles = reactive({});
export const sharedByMeFiles = reactive({});
export const sharedWithMeFiles = reactive({});